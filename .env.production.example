# Production Environment Configuration Template
# Copy this to .env.production and update with your values

# WSG API Configuration
WSG_CERT_PATH=certificates/cert.pem
WSG_KEY_PATH=certificates/key.pem
WSG_BASE_URL=https://api.ssg-wsg.sg

# Server Configuration
HOST=0.0.0.0
PORT=8080
ENVIRONMENT=production
LOG_LEVEL=INFO

# Workers (for multi-process servers)
WORKERS=4

# CORS Configuration (comma-separated list of allowed origins)
# Leave empty to disable CORS, or specify allowed domains
CORS_ORIGINS=

# Optional: API Key for securing your server
# API_KEY=your-secret-api-key-here

# Cloud Run specific (auto-detected)
# K_SERVICE, K_REVISION, K_CONFIGURATION are set by Cloud Run

# Optional: Database connection (if you add database later)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# Optional: Redis cache (if you add caching)
# REDIS_URL=redis://localhost:6379

# Optional: Sentry error tracking
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project

# Optional: Rate limiting
# RATE_LIMIT=100/minute
